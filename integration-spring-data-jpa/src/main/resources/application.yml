server:
  port: 8090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/boot_security_integration_jpa?useUnicode=true&serverTimezone=Asia/Shanghai&autoReconnect=true&useSSL=false&characterEncoding=UTF-8
    username: root
    password: Qt123456@
  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL57Dialect
    database: mysql
    open-in-view: true
